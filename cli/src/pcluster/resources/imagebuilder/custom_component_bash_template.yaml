name: CustomComponent
description: CustomComponent
schemaVersion: 1.0

phases:
  - name: build
    steps:
      # bash script
      - name: bash script
        action: ExecuteBash
        inputs:
          commands:
            - |
              {{ place-holder }}

